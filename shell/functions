ripdvd() {
	lsdvd
	read nr
	for i in $(jot ${nr} 2); do mplayer -dumpstream -dumpfile \
		Nikke_Knatterton-${i}.vob dvd://$i ; done
}

sprunge() {
	printf '%s%s\n' "$(curl -sF 'sprunge=<-' \
		http://sprunge.us/)" "${*:+?$*}";
}

